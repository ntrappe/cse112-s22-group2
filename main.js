/**
 * @method
 * To print out hello world some number of times
 * @param {Number} how many times to print
 */
helloWorld = (n) => {
    for (let i = 0; i < n; i++)
        console.log ("Hello World!");
}

helloWorld (2);